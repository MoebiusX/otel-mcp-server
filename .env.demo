# Demo Environment Configuration
# Copy to .env and modify as needed

# Demo Mode Settings
DEMO_MODE=true

# Infrastructure URLs (match docker-compose.yml)
DATABASE_URL=postgresql://krystaline:krystaline@localhost:5432/krystaline
RABBITMQ_URL=amqp://guest:guest@localhost:5672
REDIS_URL=redis://localhost:6379
KONG_URL=http://localhost:8000
JAEGER_URL=http://localhost:16686

# JWT (use these demo-safe values)
JWT_SECRET=demo-secret-change-in-production-32chars!!
JWT_ACCESS_EXPIRY=1h
JWT_REFRESH_EXPIRY=7d

# Email (MailDev for demo - no real emails sent)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_FROM=noreply@krystaline.io

# Binance API (real prices, but rate-limited)
BINANCE_API_URL=https://api.binance.com

# Whale Detection (amount anomaly detection)
ENABLE_AMOUNT_ANOMALY_DETECTION=true

# Demo User Credentials (for quick access)
# Email: demo@krystaline.io
# Password: Demo123!
# 2FA Code: 123456 (if prompted)

# Pre-Demo Checklist (see scripts/prepare-demo.js)
# 1. docker compose up -d
# 2. Wait 60 seconds for services
# 3. node scripts/prepare-demo.js
# 4. npm run dev
