node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment files (may contain secrets)
.env
.env.local
.env.*.local
# Allow example files (they should never contain real secrets)
!.env.example
!.env.*.example

# Logs
logs.txt
*.log
npm-debug.log*

# IDE
.vscode/
.idea/
*.swp
*.swo

# Test artifacts
playwright-report/
test-results/

# OS
Thumbs.db

# Secrets (never commit real secrets)
secrets/
*.pem
*.key

# Confidential documentation (whitepaper, screenshots, zip bundles)
docs/OBSERVABILITY_ADOPTION_GUIDE.md
docs/OBSERVABILITY_MATHEMATICS.md
docs/images/
docs/api/
docs/*.zip
docs-bundle.zip

# Grafana dashboard exports (raw exports with timestamps, clean copies in k8s/charts/krystalinex/dashboards/)
docs/*Dashboard*.json
docs/*Observability*.json

# Model training artifacts (large binary files)
lora-anomaly-analyzer/
last_run_prepared/
*.gguf
*.safetensors
*.bin

# Confidential
docs/bizait lab network.md

values-secrets.yaml

# VPN configs
*.ovpn

# Circom compilation intermediates (large, not needed at runtime)
server/circuits/build/*.ptau
server/circuits/build/*_0.zkey
server/circuits/build/*.r1cs
server/circuits/build/*.sym
