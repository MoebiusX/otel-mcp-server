# Supported Versions

> Generated by `npm run gen:versions` from the typed protocol & version
> catalog (`src/protocols.ts`, `src/skill-versions.ts`). Do not edit by hand.

Support tiers: **must** = latest major(s) · **should** = previous · **optional** = residual tail.

## Prometheus query API (`promql`)

- **Query language:** PromQL
- **Products:** Prometheus, Grafana Mimir, Thanos, Cortex, VictoriaMetrics
- **Spec:** https://prometheus.io/docs/prometheus/latest/querying/basics/

### Supported product versions

| Skill | Backend | Tiers |
| --- | --- | --- |
| `metrics` | Cortex | must: 1.x · should: 0.x |
| `metrics` | Grafana Mimir | must: 2.14, 2.13 · should: 2.10 · optional: 2.0 |
| `metrics` | Prometheus | must: 3.x · should: 2.55, 2.5x · optional: 2.4x |
| `metrics` | Thanos | must: 0.37, 0.36 · should: 0.34 · optional: 0.30 |
| `metrics` | VictoriaMetrics | must: 1.x · should: 0.x |

### Baseline features (always available, ungated)

`sum`, `min`, `max`, `avg`, `group`, `stddev`, `stdvar`, `count`, `count_values`, `bottomk`, `topk`, `quantile`, `by`, `without`, `on`, `ignoring`, `group_left`, `group_right`, `rate`, `irate`, `increase`, `resets`, `changes`, `idelta`, `delta`, `deriv`, `predict_linear`, `holt_winters`, `abs`, `ceil`, `floor`, `round`, `exp`, `ln`, `log2`, `log10`, `sqrt`, `clamp`, `clamp_min`, `clamp_max`, `sgn`, `time`, `timestamp`, `minute`, `hour`, `day_of_week`, `day_of_month`, `day_of_year`, `days_in_month`, `month`, `year`, `label_replace`, `label_join`, `histogram_quantile`, `histogram_fraction`, `absent`, `absent_over_time`, `vector`, `scalar`, `sort`, `sort_desc`, `avg_over_time`, `min_over_time`, `max_over_time`, `sum_over_time`, `count_over_time`, `quantile_over_time`, `stddev_over_time`, `stdvar_over_time`, `last_over_time`, `present_over_time`

### Versioned features — since-version by product

| Feature | Cortex | Grafana Mimir | Prometheus | Thanos | VictoriaMetrics |
| --- | --- | --- | --- | --- | --- |
| `subqueries`<br/><sub>Subquery range selectors `expr[5m:1m]`.</sub> | 1.0 | 2.0 | 2.7 | 0.10 | 1.0 |
| `at_modifier`<br/><sub>`@` modifier to fix evaluation timestamp.</sub> | 1.11 | 2.0 | 2.26 | 0.21 | 1.54 |
| `negative_offset`<br/><sub>Negative `offset` to look ahead.</sub> | 1.11 | 2.0 | 2.26 | 0.21 | 1.54 |
| `native_histograms`<br/><sub>Native (sparse) histogram queries.</sub> | — | 2.10 | 3.0 | 0.34 | — |
| `utf8_names`<br/><sub>UTF-8 metric and label names.</sub> | — | 2.14 | 3.0 | 0.37 | — |
| `sort_by_label`<br/><sub>`sort_by_label` / `sort_by_label_desc`.</sub> | — | 2.13 | 2.55 | 0.36 | 1.93 |
| `info_func`<br/><sub>`info()` function for metadata joins.</sub> | — | — | 3.0 | — | — |
| `double_exp_smoothing`<br/><sub>`double_exponential_smoothing` (renamed from `holt_winters` in 3.0).</sub> | — | — | 3.0 | — | — |

## Loki query API (`logql`)

- **Query language:** LogQL
- **Products:** Grafana Loki
- **Spec:** https://grafana.com/docs/loki/latest/query/

### Supported product versions

| Skill | Backend | Tiers |
| --- | --- | --- |
| `logs` | Grafana Loki | must: 3.x · should: 2.9 · optional: 2.8 |

### Baseline features (always available, ungated)

`stream_selector`, `line_filter_eq`, `line_filter_neq`, `line_filter_regex`, `json`, `logfmt`, `regexp`, `unpack`, `line_format`, `label_format`, `label_filter`, `rate`, `count_over_time`, `bytes_rate`, `bytes_over_time`, `sum`, `avg`, `min`, `max`, `stddev`, `stdvar`, `count`, `topk`, `bottomk`, `quantile_over_time`, `first_over_time`, `last_over_time`, `absent_over_time`, `sum_over_time`, `avg_over_time`, `min_over_time`, `max_over_time`, `unwrap`, `by`, `without`

### Versioned features — since-version by product

| Feature | Grafana Loki |
| --- | --- |
| `ip_filter`<br/><sub>IP-address matching with `ip()`.</sub> | 2.5 |
| `pattern_parser`<br/><sub>`pattern` parser for structured extraction.</sub> | 2.9 |
| `drop_stage`<br/><sub>`drop` label stage.</sub> | 2.9 |
| `decolorize`<br/><sub>`decolorize` stage to strip ANSI codes.</sub> | 2.9 |
| `structured_metadata`<br/><sub>Query over structured metadata.</sub> | 3.0 |
| `keep_stage`<br/><sub>`keep` label stage.</sub> | 3.0 |
| `distinct`<br/><sub>`distinct` label filter.</sub> | 3.1 |

## Elasticsearch Query DSL (`esdsl`)

- **Query language:** Query DSL
- **Products:** Elasticsearch, OpenSearch, Quickwit, Elastic APM
- **Spec:** https://www.elastic.co/guide/en/elasticsearch/reference/current/query-dsl.html

### Supported product versions

| Skill | Backend | Tiers |
| --- | --- | --- |
| `elasticsearch` | Elasticsearch | must: 9.x · should: 8.x · optional: 7.x |
| `elasticsearch` | OpenSearch | must: 2.x · should: 1.x |

### Baseline features (always available, ungated)

`match`, `match_phrase`, `match_phrase_prefix`, `multi_match`, `term`, `terms`, `terms_set`, `range`, `exists`, `prefix`, `wildcard`, `regexp`, `fuzzy`, `ids`, `query_string`, `simple_query_string`, `bool`, `dis_max`, `boosting`, `constant_score`, `function_score`, `nested`, `avg`, `sum`, `min`, `max`, `stats`, `extended_stats`, `value_count`, `cardinality`, `percentiles`, `percentile_ranks`, `top_hits`, `geo_bounds`, `terms_agg`, `date_histogram`, `histogram`, `range_agg`, `date_range`, `filters`, `significant_terms`, `geo_distance`, `missing`, `global`, `sampler`, `derivative`, `cumulative_sum`, `moving_fn`, `bucket_script`, `bucket_selector`, `serial_diff`, `avg_bucket`, `max_bucket`, `min_bucket`, `sum_bucket`, `sort`, `source_filtering`, `highlight`, `paging`, `collapse`, `track_total_hits`

### Versioned features — since-version by product

| Feature | Elasticsearch | OpenSearch |
| --- | --- | --- |
| `search_after`<br/><sub>Deep pagination via `search_after`.</sub> | 5.0 | 1.0 |
| `composite_aggs`<br/><sub>Composite aggregation for paginated buckets.</sub> | 6.1 | 1.0 |
| `async_search`<br/><sub>Async search API.</sub> | 7.7 | — |
| `pit_search`<br/><sub>Point-in-time search context.</sub> | 7.10 | 2.4 |
| `runtime_fields`<br/><sub>Runtime (schema-on-read) fields.</sub> | 7.11 | — |
| `esql`<br/><sub>ES|QL piped query language.</sub> | 8.14 | — |

## Jaeger Query API (`jaeger`)

- **Query language:** Jaeger Query
- **Products:** Jaeger, Grafana Tempo (Jaeger-compatible)
- **Spec:** https://www.jaegertracing.io/docs/latest/apis/

### Supported product versions

| Skill | Backend | Tiers |
| --- | --- | --- |
| `traces` | Jaeger | must: 2.x · should: 1.6x, 1.5x · optional: 1.4x |

### Baseline features (always available, ungated)

`search_by_service`, `search_by_operation`, `search_by_tags`, `get_trace_by_id`, `lookback_window`, `limit`, `min_duration`, `max_duration`

### Versioned features — since-version by product

| Feature | Jaeger |
| --- | --- |
| `services_list`<br/><sub>List known services.</sub> | 1.0 |
| `operations_list`<br/><sub>List operations for a service.</sub> | 1.0 |
| `dependencies_graph`<br/><sub>Service dependency graph (needs dependency job).</sub> | 1.0 |
| `otlp_native_storage`<br/><sub>Native OTLP ingestion path.</sub> | 1.35 |
| `clickhouse_storage_v2`<br/><sub>Jaeger v2 (OpenTelemetry-collector backed).</sub> | 2.0 |

## Tempo TraceQL API (`traceql`)

- **Query language:** TraceQL
- **Products:** Grafana Tempo
- **Spec:** https://grafana.com/docs/tempo/latest/traceql/

### Supported product versions

| Skill | Backend | Tiers |
| --- | --- | --- |
| `traces` | Tempo | must: 2.7, 2.6 · should: 2.5 · optional: 2.4 |

### Baseline features (always available, ungated)

`get_trace_by_id`, `span_attribute_filter`, `resource_attribute_filter`, `duration_filter`, `limit`

### Versioned features — since-version by product

| Feature | Tempo |
| --- | --- |
| `traceql_basic`<br/><sub>Basic span selectors `{ ... }`.</sub> | 2.0 |
| `traceql_structural`<br/><sub>Structural operators (descendant/child/sibling).</sub> | 2.2 |
| `traceql_aggregates`<br/><sub>Aggregates over matched spans.</sub> | 2.3 |
| `metrics_from_traces`<br/><sub>TraceQL metrics (rate/quantile_over_time).</sub> | 2.4 |
| `traceql_instrument`<br/><sub>Exemplars / instrumented results.</sub> | 2.6 |

## Zipkin API v2 (`zipkin`)

- **Query language:** Zipkin Query
- **Products:** Zipkin
- **Spec:** https://zipkin.io/zipkin-api/

### Supported product versions

| Skill | Backend | Tiers |
| --- | --- | --- |
| `traces` | Zipkin | must: 3.x · should: 2.x |

### Baseline features (always available, ungated)

`search`, `get_trace_by_id`, `services_list`, `span_names`, `dependencies`, `lookback`, `limit`, `min_duration`

### Versioned features — since-version by product

| Feature | Zipkin |
| --- | --- |
| `api_v2`<br/><sub>Zipkin API v2 (v1 endpoints are unsupported).</sub> | 2.0 |

## SkyWalking OAP GraphQL (`skywalking`)

- **Query language:** GraphQL
- **Products:** Apache SkyWalking
- **Spec:** https://skywalking.apache.org/docs/

### Supported product versions

| Skill | Backend | Tiers |
| --- | --- | --- |
| `traces` | SkyWalking | must: 10.x · should: 9.x · optional: 8.x |

### Baseline features (always available, ungated)

`trace_query`, `get_trace_by_id`, `services_list`, `endpoints_list`, `topology`, `dependencies`, `duration_window`

### Versioned features — since-version by product

| Feature | SkyWalking |
| --- | --- |
| `graphql_v3_schema`<br/><sub>Current GraphQL query-protocol schema.</sub> | 9.0 |
| `logs_correlation`<br/><sub>Trace↔log correlation queries.</sub> | 9.2 |
| `ebpf_profiling_query`<br/><sub>eBPF profiling queries.</sub> | 9.3 |

## InfluxDB InfluxQL API (`influxql`)

- **Query language:** InfluxQL
- **Products:** InfluxDB 1.x, InfluxDB 2.x (compat)
- **Spec:** https://docs.influxdata.com/influxdb/v1/query_language/

### Supported product versions

| Skill | Backend | Tiers |
| --- | --- | --- |
| `influx` | InfluxDB 1.x | must: 1.11 · should: 1.8 |

### Baseline features (always available, ungated)

`select`, `where`, `group_by_time`, `group_by_tag`, `fill`, `mean`, `median`, `mode`, `sum`, `count`, `min`, `max`, `first`, `last`, `spread`, `stddev`, `percentile`, `distinct`, `top`, `bottom`, `sample`, `derivative`, `non_negative_derivative`, `difference`, `moving_average`, `cumulative_sum`, `elapsed`, `into`, `show_measurements`, `show_tag_keys`, `show_field_keys`, `show_series`

## InfluxDB Flux API (`flux`)

- **Query language:** Flux
- **Products:** InfluxDB 2.x
- **Spec:** https://docs.influxdata.com/flux/v0/

### Supported product versions

| Skill | Backend | Tiers |
| --- | --- | --- |
| `influx` | InfluxDB 2.x | must: 2.7 · should: 2.6 |

### Baseline features (always available, ungated)

`from`, `range`, `filter`, `group`, `aggregateWindow`, `map`, `pivot`, `join`, `to`

### Versioned features — since-version by product

| Feature | InfluxDB 2.x |
| --- | --- |
| `flux_read`<br/><sub>Flux read queries (removed in InfluxDB 3.x).</sub> | 2.0 |

## InfluxDB 3 SQL/FlightSQL API (`influx_sql`)

- **Query language:** SQL
- **Products:** InfluxDB 3 Core, InfluxDB 3 Enterprise
- **Spec:** https://docs.influxdata.com/influxdb3/core/query-data/sql/

### Supported product versions

| Skill | Backend | Tiers |
| --- | --- | --- |
| `influx` | InfluxDB 3.x | must: 3.x |

### Baseline features (always available, ungated)

`select`, `where`, `group_by`, `having`, `order_by`, `window_functions`, `information_schema`

### Versioned features — since-version by product

| Feature | InfluxDB 3.x |
| --- | --- |
| `sql_query`<br/><sub>ANSI SQL over measurements-as-tables.</sub> | 3.0 |
| `flightsql_transport`<br/><sub>FlightSQL transport.</sub> | 3.0 |
| `date_bin_windowing`<br/><sub>`date_bin()` time-bucketing.</sub> | 3.0 |
| `last_cache`<br/><sub>Last-value / distinct-value cache.</sub> | 3.0 |

## ClickHouse HTTP SQL (`clickhouse`)

- **Query language:** SQL
- **Products:** ClickHouse
- **Spec:** https://clickhouse.com/docs/en/sql-reference

### Supported product versions

| Skill | Backend | Tiers |
| --- | --- | --- |
| `clickhouse` | ClickHouse | must: 25.x, 24.x · should: 24.3 · optional: 23.8 |

### Baseline features (always available, ungated)

`select`, `where`, `group_by`, `order_by`, `limit`, `join`, `cte`, `array_join`, `subqueries`, `format_json`, `system_tables`, `count`, `sum`, `avg`, `min`, `max`, `uniq`, `uniqExact`, `quantile`, `quantiles`, `groupArray`, `datetime_functions`, `show_tables`, `show_databases`, `describe`

### Versioned features — since-version by product

| Feature | ClickHouse |
| --- | --- |
| `analyzer_new`<br/><sub>New query analyzer (default-on in 24.8).</sub> | 24.3 |
| `parallel_replicas`<br/><sub>Parallel replicas for distributed reads.</sub> | 23.11 |
| `json_object_type`<br/><sub>Native JSON data type.</sub> | 24.8 |
| `query_condition_cache`<br/><sub>Query condition cache.</sub> | 25.1 |

## Graylog search API (`graylog`)

- **Query language:** Lucene
- **Products:** Graylog
- **Spec:** https://go2docs.graylog.org/current/what_is_graylog/what_is_graylog.html

### Supported product versions

| Skill | Backend | Tiers |
| --- | --- | --- |
| `graylog` | Graylog | must: 6.x · should: 5.2 · optional: 5.0, 4.x |

### Baseline features (always available, ungated)

`full_text`, `field_value`, `boolean_ops`, `range`, `wildcards`, `exists`, `timerange`, `sort`, `paging`, `streams_filter`, `fields_projection`, `message_by_id`, `inputs_listing`, `streams_listing`

### Versioned features — since-version by product

| Feature | Graylog |
| --- | --- |
| `views_search_api`<br/><sub>POST /views/search aggregation API.</sub> | 4.0 |
| `pivot_aggregations`<br/><sub>Pivot-style aggregations.</sub> | 4.0 |
| `parameters_in_views`<br/><sub>Parameterised views (Ops/Enterprise).</sub> | 5.0 |
| `data_tiering`<br/><sub>Data tiering / warm tier.</sub> | 5.2 |
| `search_filters_reusable`<br/><sub>Reusable saved search filters.</sub> | 6.0 |

## OpenTSDB HTTP query API (`opentsdb`)

- **Query language:** OpenTSDB Query
- **Products:** OpenTSDB
- **Spec:** http://opentsdb.net/docs/build/html/api_http/index.html

### Supported product versions

| Skill | Backend | Tiers |
| --- | --- | --- |
| `opentsdb` | OpenTSDB | must: 2.4.x · should: 2.3.x |

### Baseline features (always available, ungated)

`query`, `sum`, `avg`, `min`, `max`, `count`, `dev`, `zimsum`, `mimmin`, `mimmax`, `downsampling`, `rate`, `suggest`, `search_lookup`, `multi_metric`, `time_range`

### Versioned features — since-version by product

| Feature | OpenTSDB |
| --- | --- |
| `fill_policies_downsample`<br/><sub>Downsample fill policies (none/nan/null/zero).</sub> | 2.2 |
| `explicit_tags`<br/><sub>Explicit-tags query optimisation.</sub> | 2.3 |
| `rollup_preagg_queries`<br/><sub>Rollup / pre-aggregate queries.</sub> | 2.4 |
| `histograms_sketches`<br/><sub>Histogram & sketch data types.</sub> | 2.4 |
| `exp_expression_query`<br/><sub>`/api/query/exp` expression queries.</sub> | 2.3 |

## Pyroscope query API (`pyroscope`)

- **Query language:** Pyroscope Query
- **Products:** Grafana Pyroscope
- **Spec:** https://grafana.com/docs/pyroscope/latest/

### Supported product versions

| Skill | Backend | Tiers |
| --- | --- | --- |
| `pyroscope` | Grafana Pyroscope | must: 1.x · should: 0.x |

### Baseline features (always available, ungated)

`profile_by_app`, `label_selector`, `time_range`, `flamegraph`, `profile_types`, `render_json`, `flamebearer`, `label_names`, `label_values`

### Versioned features — since-version by product

| Feature | Grafana Pyroscope |
| --- | --- |
| `render_api_legacy`<br/><sub>Legacy `/render` API (pre-1.0).</sub> | 0.1 |
| `connect_query_api`<br/><sub>Connect querier.v1 API (post-Phlare merge).</sub> | 1.0 |
| `select_merge_profile`<br/><sub>`SelectMergeProfile` query.</sub> | 1.0 |
| `flamegraph_diff`<br/><sub>Flamegraph diff (`render-diff`).</sub> | 1.1 |
| `relabeling_query`<br/><sub>Relabeling at query time.</sub> | 1.2 |
| `span_profiles`<br/><sub>Span profiles (trace→profile link).</sub> | 1.3 |

## OPA Data & Decision API (`opa-rest`)

- **Query language:** Rego
- **Products:** Open Policy Agent
- **Spec:** https://www.openpolicyagent.org/docs/latest/rest-api/

### Supported product versions

| Skill | Backend | Tiers |
| --- | --- | --- |
| `opa` | Open Policy Agent | must: 1.x · should: 0.6x · optional: 0.5x |

### Baseline features (always available, ungated)

`query_data`, `post_data_input`, `list_policies`, `get_policy`, `health`, `metrics`

### Versioned features — since-version by product

| Feature | Open Policy Agent |
| --- | --- |
| `partial_eval_compile`<br/><sub>Partial evaluation via `/v1/compile`.</sub> | 0.11 |
| `decision_logs_api`<br/><sub>Decision logs API.</sub> | 0.13 |
| `bundle_status_api`<br/><sub>Bundle status API.</sub> | 0.13 |
| `rego_v1_syntax`<br/><sub>Rego v1 syntax (`if`/`contains`; default in 1.0).</sub> | 0.59 |
| `strict_builtin_errors`<br/><sub>Strict built-in error handling.</sub> | 1.0 |

## Grafana HTTP API (`grafana-http`)

- **Query language:** Grafana HTTP
- **Products:** Grafana
- **Spec:** https://grafana.com/docs/grafana/latest/developers/http_api/

### Supported product versions

| Skill | Backend | Tiers |
| --- | --- | --- |
| `grafana` | Grafana | must: 12.x, 13.x · should: 11.x · optional: 10.x, 9.x |

### Baseline features (always available, ungated)

`health`, `search_dashboards`, `get_dashboard`, `list_datasources`, `list_folders`, `list_alert_rules`, `list_users`, `list_teams`, `datasource_proxy`

## Alertmanager API v2 (`alertmanager-http`)

- **Query language:** Alertmanager HTTP
- **Products:** Alertmanager
- **Spec:** https://github.com/prometheus/alertmanager/blob/main/api/v2/openapi.yaml

### Supported product versions

| Skill | Backend | Tiers |
| --- | --- | --- |
| `alertmanager` | Alertmanager | must: 0.28, 0.27 · should: 0.26 · optional: 0.25 |

### Baseline features (always available, ungated)

`alerts`, `silences`, `status`, `receivers`

## Kubernetes core API (`k8s-core`)

- **Query language:** Kubernetes API
- **Products:** Kubernetes
- **Spec:** https://kubernetes.io/docs/reference/kubernetes-api/

### Supported product versions

| Skill | Backend | Tiers |
| --- | --- | --- |
| `kubernetes` | Kubernetes | must: 1.33, 1.32 · should: 1.31 · optional: 1.30 |

### Baseline features (always available, ungated)

`list_pods`, `list_nodes`, `list_namespaces`, `list_events`, `get_resource`

## Envoy admin API (`envoy-admin`)

- **Query language:** Envoy Admin
- **Products:** Envoy
- **Spec:** https://www.envoyproxy.io/docs/envoy/latest/operations/admin

### Supported product versions

| Skill | Backend | Tiers |
| --- | --- | --- |
| `envoy` | Envoy | must: 1.33, 1.32 · should: 1.31 · optional: 1.30 |

### Baseline features (always available, ungated)

`server_info`, `stats`, `clusters`, `config_dump`

## Consul HTTP API (`consul-http`)

- **Query language:** Consul HTTP
- **Products:** Consul
- **Spec:** https://developer.hashicorp.com/consul/api-docs

### Supported product versions

| Skill | Backend | Tiers |
| --- | --- | --- |
| `consul` | Consul | must: 1.20, 1.19 · should: 1.18 · optional: 1.17 |

### Baseline features (always available, ungated)

`health`, `services`, `nodes`, `kv`, `catalog`

## Kong admin API (`kong-admin`)

- **Query language:** Kong Admin
- **Products:** Kong
- **Spec:** https://docs.konghq.com/gateway/latest/admin-api/

### Supported product versions

| Skill | Backend | Tiers |
| --- | --- | --- |
| `kong` | Kong | must: 3.8, 3.7 · should: 3.6 · optional: 3.4 |

### Baseline features (always available, ungated)

`status`, `services`, `routes`, `plugins`

## Traefik API (`traefik-http`)

- **Query language:** Traefik HTTP
- **Products:** Traefik
- **Spec:** https://doc.traefik.io/traefik/operations/api/

### Supported product versions

| Skill | Backend | Tiers |
| --- | --- | --- |
| `traefik` | Traefik | must: 3.x · should: 2.11 · optional: 2.10 |

### Baseline features (always available, ungated)

`overview`, `routers`, `services`, `middlewares`

## Pinpoint web API (`pinpoint-http`)

- **Query language:** Pinpoint HTTP
- **Products:** Pinpoint
- **Spec:** https://pinpoint-apm.gitbook.io/pinpoint/

### Supported product versions

| Skill | Backend | Tiers |
| --- | --- | --- |
| `pinpoint` | Pinpoint | must: 3.x · should: 2.5 · optional: 2.4 |

### Baseline features (always available, ungated)

`applications`, `agents`, `server_map`, `transactions`

## Cilium agent API (`cilium-http`)

- **Query language:** Cilium HTTP
- **Products:** Cilium
- **Spec:** https://docs.cilium.io/en/stable/api/

### Supported product versions

| Skill | Backend | Tiers |
| --- | --- | --- |
| `cilium` | Cilium | must: 1.17, 1.16 · should: 1.15 · optional: 1.14 |

### Baseline features (always available, ungated)

`health`, `endpoints`, `policy`, `status`

## Fluent Bit monitoring API (`fluentbit-http`)

- **Query language:** Fluent Bit HTTP
- **Products:** Fluent Bit
- **Spec:** https://docs.fluentbit.io/manual/administration/monitoring

### Supported product versions

| Skill | Backend | Tiers |
| --- | --- | --- |
| `pipeline` | Fluent Bit | must: 3.x · should: 2.2 · optional: 2.1 |

### Baseline features (always available, ungated)

`uptime`, `metrics`, `health`

## Beats monitoring API (`beats-http`)

- **Query language:** Beats HTTP
- **Products:** Filebeat, Metricbeat
- **Spec:** https://www.elastic.co/guide/en/beats/filebeat/current/http-endpoint.html

### Supported product versions

| Skill | Backend | Tiers |
| --- | --- | --- |
| `pipeline` | Beats | must: 9.x, 8.x · should: 8.x · optional: 7.x |

### Baseline features (always available, ungated)

`stats`, `state`, `info`

## Vector GraphQL/HTTP API (`vector-http`)

- **Query language:** Vector GraphQL
- **Products:** Vector
- **Spec:** https://vector.dev/docs/reference/api/

### Supported product versions

| Skill | Backend | Tiers |
| --- | --- | --- |
| `pipeline` | Vector | must: 0.4x · should: 0.3x |

### Baseline features (always available, ungated)

`health`, `components`, `metrics`

## Grafana Alloy API (`alloy-http`)

- **Query language:** Alloy HTTP
- **Products:** Grafana Alloy
- **Spec:** https://grafana.com/docs/alloy/latest/

### Supported product versions

| Skill | Backend | Tiers |
| --- | --- | --- |
| `pipeline` | Grafana Alloy | must: 1.x |

### Baseline features (always available, ungated)

`ready`, `components`, `metrics`

## Zero-knowledge proof protocol (`zk-native`)

- **Query language:** ZK Protocol
- **Products:** ZK Proofs

### Supported product versions

| Skill | Backend | Tiers |
| --- | --- | --- |
| `zk-proofs` | ZK Proofs | must: 1.x |

### Baseline features (always available, ungated)

`generate_proof`, `verify_proof`

## Application API (internal) (`app-api`)

- **Query language:** App API
- **Products:** Application API

### Supported product versions

| Skill | Backend | Tiers |
| --- | --- | --- |
| `system` | Application API | must: 1.x |

### Baseline features (always available, ungated)

`health`, `info`

